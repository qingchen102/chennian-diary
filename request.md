# 📓 手札日记 · 项目需求提示词文档

## 一、项目概述
创建一个 **数字手札（Digital Handwritten Journal）**，它模拟真实纸质笔记本的视觉与交互体验，支持图文混排、灵活日期记录、旁注批注和个性化定制。项目基于 **DeepSeek Harness (DSH)** 开发，充分利用其插件化架构和本地文件系统能力，构建一个离线优先、质感十足的个人生活记录工具。

---

## 二、核心功能需求

### 1. 日期记录（灵活格式）
- 用户可选择两种日期模式：
  - **标准公历**：自动生成“公元 XXXX 年 XX 月 XX 日”。
  - **事件计时**：自定义事件名称（如“放假Day1”），系统自动递增计数。
- 日期显示在每篇日记的第一行，作为头部。

### 2. 元信息记录
- 每篇日记包含以下字段，位于日期下方：
  - **天气**（下拉选择：晴、多云、雨、雪等，或自由输入）
  - **地点**（自由输入）
  - **标题**（必填，简短概括）

### 3. 正文书写
- 正文支持 **富文本**（粗体、斜体、列表、引用、标题层级）。
- 支持 **插入图片**，图片可放置于：
  - **正文内联**（随文本流动）
  - **页面底部**（作为附件相册）

### 4. 旁注批注（重点交互）
- 在**阅读模式**下，用户可 **双击/长按** 任意段落文字，弹出“添加旁注”按钮。
- 旁注默认**隐藏**，仅在被点击展开时才显示。
- 旁注内容支持文字和图片，且自动附上 **当前系统日期和时间**（YYYY-MM-DD HH:MM）。
- 旁注显示在该段文字 **右侧或下方**（模拟书本旁注），有独立的闭合按钮，点击可收起。

### 5. 字体与颜色个性化
- **字体**：内置至少 **5 种以上**字体（手写体、宋体、黑体、楷体等），用户可自由切换，偏好自动保存。
- **文字颜色**：提供 **至少 6 种预设颜色**（如黑、深灰、深蓝、深绿、棕红、紫色），用户可在书写时选择当前文字颜色，偏好可保存。

### 6. 书本式入口与翻页导航（全新交互）
- **主界面**：一打开应用，展示 **两本实体书**（有立体感、书脊、封面纹理）：
  - 一本标注 **“日记本”**
  - 另一本标注 **“随笔本”**
- 点击任意一本书，进入 **翻页阅读模式**：
  - 书本以 **真实翻页动画**（卷曲、翻转效果）打开，显示该本子的 **当前页**。
  - **无“新建”或“回顾”等按钮**，所有操作通过 **翻页** 完成：
    - **往前翻**（向左翻）：查看更早的日记，**自然进入回顾**。
    - **往后翻**（向右翻）：如果当前是最后一篇，页面底部显示一个 **“+”号**，点击进入 **书写新日记**。
    - 如果当前不是最后一篇，往后翻就是查看更新的日记。
  - 翻页动画模拟真实纸张卷曲，有翻页音效（可选）。
  - 页码显示在页面角落。

### 7. 书写与编辑
- 点击“+”号后，当前页面变为 **可编辑状态**：
  - 日期、天气、地点、标题区变为可编辑。
  - 正文区变为富文本编辑器。
  - 保存后，该页作为新日记插入到当前本子的最后一篇之后。
- 已写页面在翻到时显示为 **只读状态**，但可通过 **长按文字** 触发旁注添加，或通过 **页面边缘的隐藏按钮** 进入编辑模式（修改内容）。

### 8. 图片与旁注的可见性
- 图片在阅读模式下 **不直接展开**，而是显示为 **缩略图或图标**，点击后弹出查看。
- 旁注同样 **默认隐藏**，通过点击段落旁的 **批注标记**（如小圆圈或+号）展开/收起。

---

## 三、UI/UX 设计要求

### 1. 质感与真实感
- **整体风格**：高度拟物，模拟真实书本和纸张。
- **背景**：木质桌面或布纹作为书架背景，书本封面有纹理（皮革、布面）。
- **纸张**：米白/泛黄，带有细微噪点或纹理，边缘有自然阴影，模拟纸张厚度。
- **字体**：行距宽松，段落首行缩进，符合中文书籍排版习惯。
- **翻页动画**：使用CSS 3D变换或Canvas实现逼真的翻页卷曲效果。

### 2. 书本式导航
- 两本书并排显示在书架上，点击后展开为全屏书本视图。
- 翻页通过 **左右滑动** 或 **点击页面左右边缘** 触发。
- 页面底部显示 **当前页码/总页数**。
- 书本的 **书脊** 显示本子名称（日记本/随笔本）。

### 3. 交互反馈
- 翻页时有柔和过渡和翻页音效（可选）。
- 点击“+”号时，页面从底部弹出书写面板，或页面本身变为可编辑。
- 旁注展开/收起有淡入淡出动画。
- 所有操作反馈自然、流畅，避免生硬的现代化UI。

### 4. 布局（以单页为例）
- **顶部**：日期（左对齐），天气/地点（右对齐，小字号）。
- **中间**：标题（居中，较大）。
- **正文**：从标题下开始，自然排版。
- **底部**：如有图片附件，以小图标形式排列在页脚，点击放大查看。
- **旁注**：显示在对应段落右侧（或下方），带日期戳，可关闭。

---

## 四、数据存储与隐私
- **本地优先**：所有数据（文本、图片、旁注）存储在本地文件系统或IndexedDB。
- **数据结构**：采用 JSON 格式，每篇日记为一个对象，包含日期、元信息、正文（HTML）、旁注数组、图片引用路径等。
- **图片存储**：图片文件按日记ID分目录存放，确保可迁移。
- **导出备份**：提供“导出为ZIP”功能，包含所有JSON和图片文件。
- **无需联网**，完全离线。

---

## 五、技术实现建议（针对DSH）
- **插件架构**：
  - `diary-core`：管理日记数据的增删改查、本地存储。
  - `editor-plugin`：基于 Tiptap 或 Quill 的富文本编辑器。
  - `book-ui-plugin`：实现书本渲染、翻页动画、书架视图。
  - `annotation-plugin`：管理旁注的添加、展示和隐藏。
  - `font-color-plugin`：字体和颜色切换管理。
- **UI框架**：推荐 Svelte 或 Vue，配合 Three.js 或 CSS 3D 实现翻页效果。
- **文件操作**：使用 DSH 的 `fs` 插件读写本地文件。
- **字体加载**：通过 `@font-face` 动态加载 Google Fonts 或本地字体。

---

## 六、用户交互流程示例

### 打开应用
1. 启动 → 显示书架，两本书（日记本、随笔本）。
2. 点击“日记本” → 书本翻开动画，显示上次阅读的页面。

### 阅读与回顾
3. 点击页面左边缘 → 向前翻页（更早的日记）。
4. 点击页面右边缘 → 向后翻页（更新的日记），直到最后一页。
5. 在任意页面，长按段落 → 弹出“添加旁注” → 输入内容 → 旁注隐藏，段落旁出现标记。

### 书写新日记
6. 翻到最后一页 → 页面底部出现“+”号 → 点击 → 当前页面变为可编辑状态。
7. 填写日期、天气、地点、标题，撰写正文，可插入图片。
8. 点击保存 → 页面变为只读，新日记插入到最后，自动翻到新页面。

### 个性化设置
9. 在书架界面，点击右上角“设置”图标 → 进入设置页。
10. 选择字体下拉列表 → 切换字体 → 所有文字即时更新。
11. 选择文字颜色预设 → 书写时新文字采用该颜色（已写文字不变，除非手动修改）。

---

## 七、开发优先级
| 优先级 | 模块                   | 说明 |
|--------|------------------------|------|
| P0     | 数据模型与存储         | 日记结构、本地读写 |
| P1     | 书架与书本渲染         | 两本书展示、点击展开 |
| P1     | 翻页动画与导航         | 前后翻页、页码 |
| P2     | 富文本编辑器           | 书写、保存、展示 |
| P2     | 元信息字段             | 日期、天气、地点、标题 |
| P3     | 旁注系统               | 添加、隐藏、显示 |
| P3     | 图片插入与查看         | 缩略图、点击放大 |
| P4     | 字体与颜色切换         | 预设选项、偏好保存 |
| P5     | 导出备份               | ZIP打包 |

---

## 八、验收标准
- [ ] 打开应用显示书架，两本书清晰可辨，点击后以翻页动画打开。
- [ ] 日记本内可前后翻页，最后一页底部显示“+”号，点击进入书写。
- [ ] 书写页可填写日期（双模式）、天气、地点、标题，正文支持富文本和图片插入。
- [ ] 保存后新页面自动插入，翻页正常。
- [ ] 在阅读模式下，长按段落可添加旁注，旁注自动带时间戳，点击标记可展开/收起。
- [ ] 图片以缩略图显示，点击放大查看。
- [ ] 字体列表至少5种，切换后全局更新。
- [ ] 文字颜色至少6种，书写时生效。
- [ ] 所有数据本地存储，导出功能可用。
- [ ] 整体UI具有真实书本和纸张质感，无“人机感”。

---

*文档版本：1.0*  
*最后更新：2026-08-15*